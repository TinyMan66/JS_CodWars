function squareOrSquareRoot(array) {
  let resArr = [];
  for (i = 0; i < array.length; i++ ){
    if (Math.sqrt(array[i]) % 1 === 0){
      resArr.push(Math.sqrt(array[i]));
    } else {
      resArr.push(Math.pow(array[i], 2));
    }
  } return resArr;
}

