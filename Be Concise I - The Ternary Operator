// TODO: Refactor and shorten the function
const a = "You're a(n) ";
const describeAge = age => age <= 12?`${a}kid`:age >= 13 && age <= 17?`${a}teenager`:age >= 18 && age <= 64?`${a}adult`:`${a}elderly`;

