//Task: Given an array of numbers, return a new array of length number containing the last even numbers from the original array.

function evenNumbers(array, number) {
  return array.filter((elem) => elem % 2 == 0).slice(-number); 
}
